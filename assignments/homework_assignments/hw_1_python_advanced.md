# Домашнее задание 1: Продвинутый Python

## Цель задания
Закрепить знания по продвинутым возможностям языка Python, включая декораторы, итераторы, генераторы, работу с коллекциями, датой и временем, а также модулями os и sys.

## Задачи

### Задача 1: Декораторы (25 баллов)
Создайте декоратор `@retry`, который повторяет выполнение функции при возникновении исключения. Декоратор должен принимать параметры:
- `max_attempts` - максимальное количество попыток (по умолчанию 3)
- `delay` - задержка между попытками в секундах (по умолчанию 1)

Пример использования:
```python
@retry(max_attempts=5, delay=2)
def unreliable_function():
    import random
    if random.random() < 0.7:
        raise Exception("Неудача!")
    return "Успех!"
```

### Задача 2: Итераторы и генераторы (25 баллов)
Создайте:
1. Итератор `FibonacciIterator`, который генерирует числа Фибоначчи до заданного значения
2. Генераторную функцию `fibonacci_generator(n)`, которая возвращает первые n чисел Фибоначчи

### Задача 3: Collections (25 баллов)
Реализуйте класс `TextAnalyzer`, который:
- Принимает текст в конструкторе
- Имеет метод `most_common_words(k)` для получения k самых частых слов
- Имеет метод `word_count()` для получения счетчика слов
- Имеет метод `group_by_length()` для группировки слов по длине

### Задача 4: Работа с датой и временем (25 баллов)
Создайте класс `ScheduleManager`, который:
- Позволяет добавлять события с датой и временем
- Позволяет получать события за определенный период
- Проверяет пересечения событий
- Рассчитывает продолжительность между событиями

## Требования к оформлению

1. Все функции и классы должны иметь docstring
2. Код должен соответствовать PEP 8
3. Добавьте аннотации типов
4. Напишите простые тесты для каждой задачи

## Срок сдачи
Через 2 недели с момента выдачи задания

## Критерии оценки
- Работоспособность кода: 60%
- Следование стандартам кодирования: 20%
- Наличие документации и аннотаций: 20%

## Формат сдачи
Создайте Python-файл с решением и отправьте через систему управления обучением.